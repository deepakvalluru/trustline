{
  "name": "trustline-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run clean-client && npm run build-client && npm run move-client-build && npm run build && node ./src/index.js",
    "clean-client" : "rm -rf src/build",
    "build-client" : "cd ../trustline-ui && npm run build",
    "move-client-build" : "mv ../trustline-ui/build ../trustline-api/src/",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run clean && npm install typescript && npx tsc --project ./",
    "clean": "npx tsc --build --clean"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "ripple-lib": "^1.10.0",
    "xumm-sdk": "^1.0.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.9.1",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  }
}
